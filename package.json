{
  "name": "bilscore",
  "private": true,
  "version": "1.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "pack": "repomix --config repomix.config.json",
    "dev:pub": "vite --mode public",
    "dev:per": "vite --mode personal",
    "build:pub": "vue-tsc --noEmit && vite build --mode public",
    "build:per": "vue-tsc --noEmit && vite build --mode personal",
    "android:dev:pub": "tauri android dev -- --mode public",
    "android:dev:per": "tauri android dev -- --mode personal",
    "android:build:pub": "tauri android build -- --mode public",
    "android:build:per": "tauri android build -- --mode personal",
    "android:build": "tauri android build",
    "android:dev": "tauri android dev"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2",
    "vue": "^3.5.13",
    "vue-router": "^4.6.4"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "@vitejs/plugin-vue": "^5.2.1",
    "typescript": "~5.6.2",
    "vite": "^6.0.3",
    "vue-tsc": "^2.1.10"
  }
}